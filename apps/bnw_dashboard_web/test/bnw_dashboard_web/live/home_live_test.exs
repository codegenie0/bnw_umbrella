defmodule BnwDashboardWeb.HomeLiveTest do
  use BnwDashboardWeb.ConnCase

  # import Phoenix.LiveViewTest

  test "disconnected and connected render", %{conn: _conn} do
    # {:ok, page_live, disconnected_html} = live(conn, "/")
    # assert disconnected_html =~ "Welcome to the Beef Northwest Dashboard."
    # assert render(page_live) =~ "Welcome to the Beef Northwest Dashboard."
  end
end
